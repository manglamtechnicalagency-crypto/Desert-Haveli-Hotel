# Final Agent Evidence Matrix

No issue has cleared all gates yet. Populated as `agent-assignments.md` issues move through the loop.

| Issue ID | Developer Evidence | QA Evidence | UI Evidence | Supervisor Decision | Final Status |
|---|---|---|---|---|---|
| ISSUE-001 | HANDOFF-001 | QA-DEFECT-002 (updated 2026-07-11) + QA-PASS-001: agent-executed `auth.users` token-column query (all 8 empty string, none NULL) and `get_logs(auth)` review (pre-fix 500 at 15:14:41Z, clean 200 login at 15:22:37Z, no errors since) | N/A (no UI surface — this was a backend auth-service bug, fully covered by log + query evidence) | See DECISION-003 | **Verified** |
| ISSUE-002 | HANDOFF-002, HANDOFF-004 (server-side lockout fix) | QA-DEFECT-001 (updated 2026-07-11, re-verified against HANDOFF-004): fix confirmed genuine via code Read of `src/admin/AdminLoginPage.jsx` (calls `checkLoginLockout` on mount and pre-submit, `recordLoginAttempt` post-submit) plus independent, agent-executed DB testing — ran `record_login_attempt('vinaypalsingh085@gmail.com', false)` x5 via `execute_sql`, confirmed `is_locked` flips `true` with `locked_until` ~30s out on the 5th call and `failed_count` resets to 0; confirmed `check_login_lockout` independently reports the same locked state immediately after; confirmed a success call (`record_login_attempt(..., true)`) unconditionally clears the lock. Confirmed `admin_login_attempts` has RLS enabled with zero policies (deny-by-default) and zero `anon`/`authenticated`/`public` table grants, while `has_function_privilege` confirms both roles can `EXECUTE` both RPCs. Test row reset to clean state afterward. Original client-only defect no longer applies to current code. | Not yet run | See DECISION-004 — the accepted-risk caveat from DECISION-003 is now resolved: the lockout is genuinely server-side and QA-verified against the live DB, not just a UI affordance. Remaining gap is purely procedural (UI Testing gate), not a known defect. | Partially Verified — Development + QA gates clear, UI Testing gate pending (login screen has a real UI surface: lockout message/timer needs a visual pass once a browser is connected) |
| ISSUE-021 (renumbered from a mistaken ISSUE-020 reference in HANDOFF-003/QA-DEFECT-003–007 — see DECISION-003) | HANDOFF-003 (Stage-1 code-quality audit) | QA-PASS-002 through QA-PASS-008 (RLS enabled on all 9 tables; no anon write policies found; `is_admin()`/`current_admin_role()` SECURITY DEFINER pattern verified safe; storage `room-images` bucket policies correct; pricing date-range boundaries verified inclusive with no off-by-one; all 3 HANDOFF-003 null-safe `.replace()` fixes confirmed present in current files; `ErrorBoundary` in `main.jsx` confirmed genuine, not cosmetic) + QA-DEFECT-003 (anon can read `internal_note`/`reason` columns on `room_price_overrides`/`room_availability` via direct REST call — RLS is row-level only) + QA-DEFECT-004 (leaked-password protection disabled) + QA-DEFECT-005 (6 performance advisories, low severity) + QA-DEFECT-006 (pricing-rule same-priority tiebreak non-deterministic) + QA-DEFECT-007 (`rooms.promotional_price` has no expiry mechanism). Note: RLS/policy checks were done via elevated-privilege SQL inspection of policy definitions, not by authenticating as anon/authenticated through the client SDK (no browser available this run). | Not yet run — blocked on browser connection (see DECISION-003) | See DECISION-003 | Partially Verified — Development + QA gates clear, UI Testing gate pending |
| ISSUE-022 | HANDOFF-004 (column-privilege fix) | QA-DEFECT-003 (updated 2026-07-11, re-verified against HANDOFF-004): fix confirmed via direct `information_schema.column_privileges` query against project `hzzcuqioxcjniimtyrwc` — `anon` has zero SELECT-privilege rows for `room_price_overrides.internal_note`, `room_availability.internal_note`, and `room_availability.reason`; `authenticated` retains SELECT on all columns of both tables. Confirmed `anon` still has SELECT on every other (non-sensitive) column of both tables, and cross-checked against `src/lib/roomsApi.js` that the app's own public fetch functions never selected the removed columns anyway, so no legitimate read path was broken. | N/A (pure backend privilege fix, no UI surface) | See DECISION-004 | **Verified** |
| ISSUE-023 | Not yet assigned — requires Supabase Dashboard, outside repo/agent tooling | QA-DEFECT-004 (see above) | N/A | See DECISION-004 | Open — Backlog, needs user action in Supabase Dashboard |
| ISSUE-024 | HANDOFF-004 (performance advisory fixes) | QA-DEFECT-005 (updated 2026-07-11, re-verified against HANDOFF-004): fresh `get_advisors(type:"performance")` pull confirms all 6 `unindexed_foreign_keys` warnings are gone (cross-checked against `pg_indexes` — all 6 new indexes exist), the `auth_rls_initplan` warning on `admin_profiles_select_self_or_super` is gone (confirmed via `pg_policy`/`pg_get_expr` that `auth.uid()` is now wrapped in a subselect, logic otherwise identical), and `multiple_permissive_policies` is gone for all 7 tables (spot-checked all 7 via `pg_policy`/`pg_get_expr`, confirmed each now has one `_admin_or_public_select` policy for role `authenticated` with `is_admin() OR <original row-filter>` and one `_public_select` policy for role `anon` with the same row-filter alone — mathematically identical to the prior two-policy OR). The 3 originally-unused indexes remain present and mostly still unused as expected (deliberately not dropped); minor note: `idx_rooms_display_order` no longer shows as unused in the fresh pull (still exists, has simply accrued real usage since HANDOFF-004's run) — natural drift, not a regression. | N/A (index/policy-definition changes, no UI surface) | See DECISION-004 | **Verified** |
| ISSUE-025 | HANDOFF-004 (deterministic tiebreak fix) | QA-DEFECT-006 (updated 2026-07-11, re-verified against HANDOFF-004): fix confirmed by direct Read of `src/lib/roomsApi.js` (`fetchPublicRooms()`, `fetchAdminRoomById()`, and `src/admin/RoomPricingPanel.jsx`'s direct query all now order the `room_pricing_rules` embed/query by `priority` desc then `id` asc) and `src/lib/pricing.js` (`getNightlyPrice()`'s sort comparator falls back to `id` string comparison on a priority tie). Traced through by hand with two hypothetical same-priority rules — the lower-`id` rule deterministically wins on every call. No live two-same-priority-rule data exists to reproduce against (0 rows in `room_pricing_rules`), so this remains a code-logic verification, consistent with the original defect's own caveat. | N/A (deterministic ordering is a data/query-layer property, fully covered by code + query verification, not dependent on visual rendering) | See DECISION-004 | **Verified** |
| ISSUE-026 | HANDOFF-004 (promo price expiry columns + logic) | QA-DEFECT-007 (updated 2026-07-11, re-verified against HANDOFF-004): fix confirmed via `list_tables(verbose)` — `rooms.promotional_price_start_date`/`promotional_price_end_date` exist as nullable `date` columns. Confirmed `getNightlyPrice()` in `src/lib/pricing.js` now gates the promotional-price branch on both dates with null-safe "unbounded" semantics (backward compatible for rooms with a promo price but no dates). Confirmed `src/admin/AdminRoomFormPage.jsx` has both new date inputs wired to `EMPTY_ROOM`/`buildPayload()`/`update()`, and `fetchPublicRooms()` selects both new columns so the public price calculation receives them. | Not yet run — new admin form fields have a real UI surface (date inputs, save/validation) that should get a visual pass once a browser is connected | See DECISION-004 | Partially Verified — Development + QA gates clear (code-level), UI Testing gate pending |
| ISSUE-027 | HANDOFF-005, HANDOFF-008, HANDOFF-010; `npm run build` passes | QA-DEFECT-008 reproduced then resolved: invalid PIN now clears, re-enables, focuses `#pin-0`, and accepts immediate retry. Valid login/dashboard, sign-out, protected-route redirect, disabled submit, and auto-advance passed. | UI-DEFECT-001 statically resolved (admin button motion disabled under reduced-motion). Runtime screenshots, viewport matrix, and computed-style validation blocked because the independent UI agent had no browser backend. | DECISION-006 | Partially Verified — UI runtime gate blocked by external test environment |
| ISSUE-028 | HANDOFF-012; removed only global floating WhatsApp markup and exclusive CSS; `npm run build` passes | QA-PASS-010 / HANDOFF-013: no floating WhatsApp anchor at 1280×720 or 390×844; contextual `wa.me` CTAs remain valid; no page or network errors. | UI-DEFECT-002 statically resolved; no remaining floating CTA, animation, or dedicated responsive CSS. Runtime screenshot validation blocked by the UI agent browser backend. | DECISION-007 | Partially Verified — UI runtime gate blocked by external test environment |

Status vocabulary: `Verified`, `Partially Verified`, `Not Reproducible`, `Blocked by External Dependency`, `Rejected`.
